exports.handler = async (event) => { const headers = {"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"Content-Type","Access-Control-Allow-Methods":"POST, OPTIONS"}; if (event.httpMethod === "OPTIONS") return { statusCode: 200, headers, body: "" }; if (event.httpMethod !== "POST") return { statusCode: 405, headers, body: JSON.stringify({ error: "Method not allowed" }) }; const API_KEY = process.env.API_KEY; try { const parsed = JSON.parse(event.body || "{}"); const endpoint = parsed.endpoint; const body = parsed.body; const method = parsed.method; if (!endpoint) return { statusCode: 400, headers, body: JSON.stringify({ error: "Missing endpoint" }) }; const OK = ["/public/v2/orders","/public/v2/inventories","/public/v2/stores","/public/v2/supplier-products","/public/v2/suppliers","/public/v2/ingredients","/public/v2/products"]; if (!OK.some(function(p){ return endpoint.startsWith(p); })) return { statusCode: 403, headers, body: JSON.stringify({ error: "Non autorise: " + endpoint }) }; const url = "https://api.inpulse.ai" + endpoint; const h = { "x-api-key": API_KEY, "Accept": "application/json", "Content-Type": "application/json" }; const m = (method || "POST").toUpperCase(); const opts = { method: m, headers: h }; if (m !== "GET" && m !== "HEAD") opts.body = JSON.stringify(body || {}); const r = await fetch(url, opts); const t = await r.text(); let d; try { d = JSON.parse(t); } catch (e) { d = { raw: t }; } return { statusCode: r.status, headers, body: JSON.stringify(d) }; } catch (err) { return { statusCode: 500, headers, body: JSON.stringify({ error: err.message }) }; } };
